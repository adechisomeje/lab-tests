{
  "name": "lab-tests-monorepo",
  "private": true,
  "description": "Repo tooling only. The published npm package is packages/typescript (name: lab-tests).",
  "license": "MIT",
  "workspaces": [
    "packages/typescript"
  ],
  "scripts": {
    "build": "npm run build --workspace lab-tests",
    "test": "npm run test --workspace lab-tests",
    "api": "node api/server.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/adechisomeje/lab-tests.git"
  }
}
